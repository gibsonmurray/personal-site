import DynamicPage from "@/app/(components)/DynamicPage"
import { Metadata } from "next"

export const metadata: Metadata = {
    title: "Photography View Transitions",
}

function PhotographyViewTransitions() {
    return <DynamicPage />
}

export default PhotographyViewTransitions
